import FloatingPanel from 'view/components/FloatingPanel';
import styled from 'styled-components';

export const Panel = styled(FloatingPanel)`
  position: absolute;
  top: 20px;
  right: 42px;
  min-width: auto;
`;
